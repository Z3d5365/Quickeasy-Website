{
  "name": "quickeasy-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site for QuickEasy Software (plain HTML/CSS/JS, no build step). This package.json exists only so `npm test` can run the dependency-free test suite; the site itself has no dependencies and no build.",
  "scripts": {
    "test": "node test/site-check.mjs"
  }
}
