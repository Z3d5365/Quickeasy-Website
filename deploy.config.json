{
  "_comment": [
    "Deploy targets for this site (web-builder-skills:website-deployment).",
    "Consumed by deploy.mjs — run it with `npm run deploy`.",
    "",
    "PRE-CUTOVER. The CloudFront distribution below is built and serving the real",
    "content, but it only answers to the staging alias. quickeasysoftware.com and www",
    "are still claimed by distribution E2RXDDD1LJ0WFC in account 673190877114 (origin:",
    "mywebsite.quickeasysoftware.com, i.e. WordPress). The cutover is `aws cloudfront",
    "associate-alias` to move those two names onto this distribution, then repointing",
    "the two alias records. The hosted zone itself does NOT move — it carries live mail",
    "and the BOS cloud endpoints."
  ],
  "domain": "quickeasysoftware.com",
  "environment": "pre-cutover",
  "aws": {
    "account": "951226407726",
    "region": "ap-southeast-1",
    "bucket": "site-quickeasysoftware-com",
    "serving": "cloudfront-oac",
    "url": "https://qes-stage.vibecraftedsoftware.com",
    "liveUrl": "https://quickeasysoftware.com",
    "cloudfrontDistributionId": "E12M2ABYP4FGX4",
    "cloudfrontDomainName": "d27nxwqhux4sy2.cloudfront.net",
    "originAccessControlId": "E1U226NAF0RAHO",
    "acmCertificateArn": "arn:aws:acm:us-east-1:951226407726:certificate/be57be25-0c04-4da0-a1b8-872032aa0b13",
    "keyValueStoreArn": "arn:aws:cloudfront::951226407726:key-value-store/7f304362-3185-4005-856f-3a96d957366a",
    "edgeFunction": "quickeasy-edge-router"
  },
  "dns": {
    "_comment": [
      "The zone lives in a second account. Assume the cross-account role to touch it:",
      "  aws --profile qes-dns route53 ...",
      "Records NOT to touch: MX, SPF, the three DKIM selector groups (google._domainkey,",
      "k2/k3 for Mailchimp, and eight random-selector SES CNAMEs), _dmarc, _amazonses,",
      "and every bos*/dev*/portal/app/docs/mail/login/lambda host."
    ],
    "account": "673190877114",
    "hostedZoneId": "Z103297610N4K9Y2JPVC8",
    "roleArn": "arn:aws:iam::673190877114:role/quickeasy_dns_access",
    "profile": "qes-dns",
    "incumbentApexDistribution": "E2RXDDD1LJ0WFC",
    "incumbentApexTarget": "d2lsofoemw2whl.cloudfront.net",
    "incumbentWwwTarget": "d57qwkc58nyzi.cloudfront.net",
    "wordpressOrigin": "mywebsite.quickeasysoftware.com"
  },
  "cacheControl": {
    "_comment": [
      "deploy.mjs invalidates /* on every deploy, which is what makes a long immutable",
      "TTL safe despite unhashed filenames — the invalidation, not the header, is what",
      "forces a re-fetch. If the distribution ever goes away, drop css/js back to a short",
      "revalidating value or a CSS fix will be invisible to returning visitors."
    ],
    "immutable": {
      "paths": ["assets/img", "assets/css", "assets/js"],
      "value": "public,max-age=31536000,immutable"
    },
    "pages": {
      "value": "public,max-age=300"
    }
  },
  "exclude": [
    ".git/*",
    ".claude/*",
    "CLAUDE.md",
    "README.md",
    "package.json",
    "*-REDIRECTS.txt",
    "test/*",
    "seo/*",
    "infra/*",
    "deploy.config.json"
  ],
  "verifyPaths": ["/", "/pricing/", "/th/", "/blog/", "/south-africa/"],
  "notes": [
    "The exclude list keeps dev artifacts off the public endpoint. Note that adding an",
    "--exclude does NOT remove a file already in the bucket: sync applies filters to the",
    "destination too, so --delete skips excluded keys. Anything already published has to",
    "be removed with an explicit `aws s3 rm <key>`.",
    "The bucket is private: Block Public Access fully on, BucketOwnerEnforced, no website",
    "hosting, readable only by this distribution via OAC. There is no S3 website endpoint",
    "any more — test through CloudFront.",
    "A /wp-content/* cache behaviour points at the WordPress origin with the AllViewer",
    "origin request policy, so indexed image URLs and third-party hot-links keep working",
    "after the cutover. Remove it once Search Console stops reporting hits, and note it",
    "depends on mywebsite.quickeasysoftware.com continuing to resolve."
  ]
}
